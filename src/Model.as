package src{	import flash.events.Event;	import flash.events.EventDispatcher;	import flash.events.ProgressEvent;	import flash.net.URLLoader;	import flash.net.URLRequest;		public class Model extends EventDispatcher	{		public var currentIndex:int = 0;		public var totalItems:int;		public var loader:URLLoader;				public static const MODEL_CHANGE:String = "modelChange";		public static const MODEL_LOADED:String = "modelLoaded";		public static const GLOSSARY_CHANGE:String = "glossaryChange";				public function Model()		{			loader = new URLLoader();			loader.addEventListener(Event.COMPLETE, dataLoaded);			loader.addEventListener(ProgressEvent.PROGRESS, dataLoading);		}				public function load(req:URLRequest):void		{			loader.load(req);		}				private function updateItem():void		{					}				protected function updateData():void		{					}		//Protected Classes are overwritten in MainModel-For this MVC we use a different technique to fire the changePage event.		protected function dataLoaded(event:Event):void		{			updateItem();			loader.removeEventListener(Event.COMPLETE, dataLoaded);			loader.removeEventListener(ProgressEvent.PROGRESS, dataLoading);		}				protected function dataLoading(e:ProgressEvent):void		{					}	}}